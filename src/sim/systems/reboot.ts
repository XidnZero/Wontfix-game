/**
 * Derelicts — stubbed for the vertical slice (README). Nothing produces a
 * 'rebooting' unit yet, so there is nothing for this to advance.
 */

import type { TickContext } from '../sim';

export function stepReboot(_ctx: TickContext): void {
  // No-op. See file header.
}
