/**
 * Reclaim — stubbed for the vertical slice (README). Nothing sets
 * `reclaimExposure` yet, so there is nothing here to act on.
 */

import type { TickContext } from '../sim';

export function stepReclaim(_ctx: TickContext): void {
  // No-op. See file header.
}
